FROM ubuntu:latest
RUN apt update && apt upgrade -y && apt install python-pip -y \
&& pip install websockets
ENV DEBIAN_FRONTEND=noninteractive
COPY ./server.py /
CMD ["python3", "/server.py"]
EXPOSE 5678

